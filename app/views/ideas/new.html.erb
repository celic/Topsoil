<%= provide(:title, "New Idea") %>

<h1>New Idea</h1>
<%= form_for @idea do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.label :private %>
	<%= f.check_box :private %>

	<%= f.submit %>
<% end %>
