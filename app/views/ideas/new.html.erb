<%= provide(:tite, "New Idea") %>
<h1>New Idea</h1>
<%= form_for @idea do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit class: 'btn btn-primary btn-large btn-custom' %>
<% end %>
