<%= provide(:title, "Search") %>
<h2>Search results for '<%= params[:search] %>'</h2>

<% if @ideas.empty? %>
	<p>Your search returned no results.</p>
<% else %>
	<p>Your search returned <%= @ideas.size %> results.</p>
	<%= will_paginate @ideas %>
	<ul class="ideas">
		<%= render @ideas %>
	</ul>
<% end %>
